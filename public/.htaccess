<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Không rewrite file/thư mục thật và API
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteCond %{REQUEST_URI} !^/api/

  RewriteRule ^ index.html [L]
</IfModule>
